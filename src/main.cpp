#include <cstdlib>

// Punto de entrada temporal.
// Se reemplazara en la etapa de integracion por el main
// del screensaver con el parseo de argumentos y el game loop.
int main() {
    return EXIT_SUCCESS;
}